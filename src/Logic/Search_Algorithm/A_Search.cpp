#pragma once

#include "SearchAlgorithm.h"

bool SearchAlgorithm::a_search(Map* map) {
	//functions:
	//top get top priority element
	//pop returns void
	// empty
	// push //does a copy push btw
	// emplace //some fancy c++ thing, creates a new object with same constructor, no copy
	return false;
}

